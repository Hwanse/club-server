# mockMvc response 출력 시 한글이 깨지는 현상을 다음과 같은 설정으로 해결
server:
  servlet:
    encoding:
      charset: UTF-8
      force-response: true
  jpa:
    properties:
      hibernate:
        show_sql: true
        #format_sql: true
        default_batch_fetch_size: 100